## 1. Page structure

- [x] 1.1 Create `index.html` with header, task form (input + Add button), list container, and empty-state element
- [x] 1.2 Create `styles.css` with light theme, accent button, list layout, completed strikethrough, empty state styling

## 2. Task logic

- [x] 2.1 Implement task state, add/complete/delete handlers, and empty-text validation in `app.js`
- [x] 2.2 Implement `render()` to sync DOM with state and toggle empty state visibility

## 3. Persistence

- [x] 3.1 Load tasks from `localStorage` on init; save after every state change
