## 1. Filter UI

- [x] 1.1 Add filter tab button group to `index.html` (All, Active, Done)
- [x] 1.2 Style tabs in `styles.css` with clear active state for projector visibility

## 2. Filter logic

- [x] 2.1 Add filter state, sessionStorage load/save, and tab click handlers in `app.js`
- [x] 2.2 Update `render()` to show filtered tasks and context-aware empty states
